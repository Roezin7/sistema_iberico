# Estado operativo NOD3 — 21 de agosto de 2026

## Resumen

NOD3 tiene tres activos relacionados, pero no deben confundirse:

| Activo | Función actual | Estado |
|---|---|---|
| BPM | Deployment operativo real para abastecimiento y distribución | En construcción y validación con cliente |
| Ibérico | Laboratorio operativo real para ventas, compras, inventario y rentabilidad | Piloto listo para fin de semana |
| NOD3 | Método, memoria, arquitectura y patrones extraíbles | En fase de descubrimiento y documentación |

## Fase actual

### Ibérico

**Fase 0: control operativo y validación de datos.**

El sistema ya puede:

- consultar e importar evidencia de Epos Now;
- mostrar ventas por producto y método de pago;
- registrar una conciliación diaria confirmada;
- manejar cuentas abiertas como excepción;
- mantener semanas con inventario de apertura y cierre;
- capturar inventario físico;
- conservar recetas versionadas;
- probar el algoritmo FIFO de forma aislada y en una reconstrucción histórica;
- ejecutar la operación principal viernes–domingo.

El modelo de lotes FIFO ya está conectado al flujo de compras confirmadas, ventas Epos
importadas y recetas validadas. La rentabilidad FIFO todavía depende de que cada producto
vendido tenga una asociación producto–receta–inventario confiable y de completar el
inventario físico de cierre. Por lo tanto, **el piloto debe validar el flujo operativo y
la calidad de los datos; la rentabilidad sólo debe presentarse como definitiva cuando la
semana tenga apertura, cierre y excepciones resueltas**.

### BPM

Sigue siendo el deployment operativo prioritario para abastecimiento. Las modificaciones logísticas y la aprobación del cliente deben permanecer separadas del trabajo de Ibérico.

### NOD3

NOD3 no debe convertirse todavía en otra aplicación grande. Su trabajo actual es extraer principios que se repiten:

1. qué información se captura;
2. quién la confirma;
3. qué evidencia se conserva;
4. qué decisión depende de ella;
5. qué proceso se repite lo suficiente para merecer software.

## Lo que se acaba de entregar

### Sprint operativo

- Inicio convertido en centro “Hoy”.
- Navegación principal reducida al flujo operativo.
- Cierre y caja conectado con conciliaciones de Epos.
- Importación y confirmación del corte diario.
- Días lunes–jueves sin tareas ni tarjetas de venta artificiales.
- Estados de error, vacío y reintento en Tareas.
- Mejoras de etiquetas y experiencia móvil.
- Silvia oculta durante operaciones críticas.

### Bloque técnico FIFO

- Modelo `inventory_lots`.
- Modelo `inventory_consumptions`.
- Migración preparada.
- Algoritmo FIFO puro y probado.
- 69 pruebas aprobadas.
- Build y typecheck aprobados.

### Bloque técnico de compras

- `POST /api/inventario/compras` registra una compra confirmada y crea un lote por línea.
- `GET /api/inventario/lotes` permite revisar existencias por lote, fecha, costo y ticket.
- Las compras repetidas por el mismo `ticket_ref` se rechazan.
- La migración conserva compras históricas y amplía las unidades a cuatro decimales.
- La pantalla de captura rápida y la cola de revisión ya están publicadas. La evidencia
  original se conserva temporalmente en PostgreSQL privado; queda pendiente migrarla a
  almacenamiento de objetos privado.

### Bloque técnico Epos → receta → FIFO

- `POST /api/inventario/consumo-epos` permite vista previa o confirmación explícita.
- Las ventas se buscan por `epos_product_id` y, como respaldo, por nombre.
- Sólo se usan recetas `validada` vigentes para la fecha de la venta.
- Una venta sin mapeo, receta o inventario suficiente queda como excepción.
- El consumo confirmado actualiza lotes, crea el libro inmutable y guarda el costo FIFO de la venta.
- La operación es idempotente: una venta ya costeada no vuelve a descontarse.
- La pantalla de revisión de excepciones ya está publicada; falta probarla con ventas Epos reales.

### Estado de despliegue — 21 de agosto

- Respaldo previo verificado en `backups/iberico-prod-pre-fifo-2026-08-20.dump` (PostgreSQL 18.4, 431 entradas).
- Migraciones FIFO, compras y consumo Epos aplicadas en producción.
- Pantalla de compras, lotes y excepciones publicada.
- La semana 63 fue reconstruida con sus cuatro tickets históricos, sin duplicar
  movimientos financieros: 104 líneas, 99 ventas costeadas, 5 excepciones y
  costo FIFO de $3,515.2268. Las excepciones son explícitas y no se rellenaron
  con números inventados.
- La reconstrucción usó una isla `historico_prueba`; la semana 64 conservó sus
  79 lotes de apertura y no fue consumida por la prueba.
- La semana 64 está abierta con apertura de $39,202.63. El reporte de solo
  lectura confirma que no tiene ventas dentro de su intervalo local todavía,
  además de compras móviles parcialmente confirmadas.
- El adaptador Epos fue corregido para aceptar `ProductId` y `DiscountValue`,
  que son los nombres observados en el reporte real. Antes esas filas quedaban
  sin ID estable y dependían del respaldo por nombre.
- También se corrigió el límite de zona horaria: los `DateTime` de Epos sin
  offset se interpretan como hora local de Ibérico, evitando mover ventas del
  domingo a la semana siguiente.
- El siguiente paso es ejecutar el runbook de la semana 64: sincronización
  diaria, conciliación de pagos, tickets completos, FIFO y cierre físico.

## Siguiente bloque técnico

### 1. Compras recibidas — validar operación real

Construir la captura mínima de una compra:

- proveedor;
- fecha;
- ticket o factura;
- foto/original;
- producto interno;
- cantidad comprada;
- presentación;
- precio total;
- costo por unidad base;
- revisión humana.

La compra confirmada debe crear un lote FIFO, no modificar silenciosamente el promedio del
catálogo. La aplicación ya captura el ticket y la confirmación administrativa es la única
que crea lotes y movimientos.

### 2. Mapeo Epos → menú → receta — completar revisión visual

Para cada producto vendido:

```text
Epos product_id/nombre
  → producto_menu
  → receta vigente
  → ingredientes
  → consumo FIFO
```

Un producto sin mapeo debe detener el cálculo de rentabilidad de esa línea y aparecer como
excepción. La API y la pantalla de revisión ya aplican esta regla; falta completar la
matriz de asociaciones con datos reales.

### 3. Consumo de ventas

Después de importar y revisar un día:

- seleccionar ventas importadas del día;
- multiplicar cantidad vendida por receta;
- consumir lotes por ingrediente;
- registrar cada consumo;
- generar faltantes explícitos;
- calcular costo FIFO y margen estimado.

La confirmación es idempotente por venta y la confirmación de un ticket se reserva
transaccionalmente para evitar que dos reintentos creen lotes o movimientos duplicados.

### 4. Inventario y merma

Al cierre de domingo:

- comparar existencia teórica contra conteo físico;
- clasificar diferencias como merma, receta, rendimiento, compra faltante o error;
- congelar el snapshot;
- usarlo como apertura de la siguiente semana.

### 5. Rentabilidad

Separar siempre:

- ventas brutas;
- descuentos;
- ventas netas;
- costo FIFO de ventas;
- merma;
- sueldos;
- gastos operativos;
- comisiones;
- utilidad operativa estimada.

El cambio de efectivo no debe presentarse como utilidad.

## Plan de trabajo para este fin de semana

### Viernes

1. Confirmar semana abierta e inventario de apertura.
2. Importar Epos.
3. Revisar productos y métodos de pago.
4. Registrar cuentas abiertas.
5. Confirmar corte.
6. Registrar compras recibidas.

### Sábado

1. Repetir el cierre diario.
2. Revisar productos sin mapeo.
3. Revisar compras y tickets.
4. No corregir manualmente datos históricos sin registrar la razón.

### Domingo

1. Confirmar corte.
2. Capturar inventario físico.
3. Revisar diferencias.
4. No cerrar la semana si faltan datos críticos sin dejar excepción.
5. Guardar evidencia del piloto.

## Criterio para avanzar

Ibérico podrá pasar de Fase 0 a Fase 1 cuando complete una semana con:

- tres cortes diarios confirmados;
- cero duplicados de importación;
- todos los productos mapeados o exceptuados;
- compras con evidencia;
- inventario de apertura y cierre;
- diferencias explicadas;
- una rentabilidad que indique claramente qué parte es real y qué parte es estimación.

## Decisiones que NOD3 debe conservar

- La evidencia precede a la automatización.
- Una importación no se convierte automáticamente en verdad contable.
- Una receta no consume inventario hasta estar vigente y mapeada.
- Un lote no se mezcla con otro por comodidad.
- Un faltante queda explícito; nunca se convierte en cero silenciosamente.
- Un patrón repetido se convierte en producto; una excepción aislada se documenta.

## Bloque implementado: compras rápidas y cierre diario — 20 de agosto de 2026

Se añadió un flujo único para capturar compras desde celular y conectarlas con
FIFO y Cierre y caja:

- foto privada del ticket asociada a la compra;
- lectura opcional asistida por IA, siempre como propuesta;
- borrador creado por empleado o administrador;
- líneas clasificables como `inventario`, `gasto` o `pendiente`;
- revisión y confirmación administrativa;
- una compra confirmada crea lotes FIFO y movimientos financieros vinculados;
- los tickets mixtos separan subtotal de inventario y subtotal de gasto;
- el folio evita duplicar un ticket ya registrado;
- compras itemizadas no son borradas por la edición del gasto manual del día;
- el cierre diario muestra compras FIFO, gastos itemizados y otros gastos no registrados;
- el módulo está disponible desde celular para empleados, mientras la
  confirmación irreversible permanece protegida para administradores.

La migración `20260820130000_compras_rapidas_cierre` fue aplicada en producción
después de crear el respaldo:

`backups/iberico-prod-pre-compras-rapidas-2026-08-20.dump`

La fotografía se conserva actualmente en la base privada para mantener el flujo
funcional inmediato. Debe migrarse a almacenamiento de objetos privado cuando se
incorpore el adaptador S3/R2; no se expone en archivos estáticos públicos.

La siguiente validación real debe usar tres tickets: uno sólo de inventario,
uno sólo de gasto y uno mixto. El resultado esperado es que cada ticket cree una
sola compra, ningún movimiento duplicado y el total del día cuadre con la salida
real de Caja o Banco.

## Verificación técnica posterior — 20 de agosto de 2026

Se corrigieron bloqueadores detectados al auditar el estado real del repositorio:

- el typecheck del cliente quedó limpio al retirar un componente manual legado que ya no
  se renderizaba y corregir la carga de compras del día;
- el selector de consumo Epos ahora trata “Hasta” como fecha inclusiva y envía el día
  siguiente como límite exclusivo, por lo que el periodo de un solo día es válido;
- una captura repetida con el mismo folio o la misma foto y fecha se rechaza antes de crear
  otra compra;
- la confirmación de una compra reclama primero el registro dentro de la transacción;
  un segundo intento recibe conflicto y no duplica lotes ni movimientos;
- un lote que llega a cero por consumo FIFO queda marcado como `agotado`.

Checks ejecutados localmente:

- `npm test`: 69 pruebas aprobadas;
- `npm run typecheck -w client`: aprobado;
- `npm run build`: cliente y servidor aprobados;
- `git diff --check`: aprobado.

Se ejecutó una auditoría de lectura contra producción: las 19 migraciones están aplicadas;
la semana 10–16 de agosto está cerrada y la 17–23 abierta; existen 3 conciliaciones diarias
confirmadas; y hay 59 productos de menú con receta validada. La base todavía tiene 0
importaciones Epos, 0 ventas Epos persistidas, 0 lotes FIFO y 0 consumos. Los 59 productos
de menú siguen sin `epos_product_id`, por lo que el puente Epos–receta aún requiere la
asociación de catálogo antes de calcular consumo o margen.

No se ejecutaron migraciones ni escrituras en producción. El siguiente paso operativo es
correr los tres tickets reales —inventario, gasto y mixto— con el respaldo previo verificado
y comprobar que cada uno produzca una sola compra, los lotes y movimientos esperados, y el
total correcto en el cierre diario.

## Auditoría de preparación para pruebas reales — 20 de agosto de 2026

### Verificaciones locales

- `npm test`: 73 pruebas aprobadas en 7 archivos;
- `npm run typecheck -w client`: aprobado;
- `npm run build`: cliente y servidor aprobados;
- `prisma generate`: aprobado;
- `git diff --check`: aprobado.

Las pruebas de compras cubren explícitamente ticket sólo de inventario, sólo de gasto,
mixto y diferencia no clasificada. Una diferencia entre las líneas y el total ya no se
convierte silenciosamente en gasto.

### Verificaciones de producción, sólo lectura

- PostgreSQL 18.4 remoto;
- la base tiene 20 migraciones en el repositorio, pero la nueva migración de integridad
  `20260820140000_integridad_compras_fifo` todavía no está aplicada;
- la semana actual es `2026-08-17` a `2026-08-23`, abierta, con snapshot de apertura 52;
- el snapshot de apertura contiene 134 líneas y 85 productos;
- no hay duplicados de lotes ni consumos huérfanos;
- existen 4 compras confirmadas con 25 líneas históricas, pero 0 lotes FIFO y 0 movimientos
  asociados a esas compras;
- la semana actual tiene 0 movimientos, 0 importaciones Epos, 0 ventas Epos y 0 consumos;
- existen 59 productos de menú y 59 recetas validadas, pero 0 productos de menú tienen
  `epos_product_id`.

### Veredicto

El código está listo para recibir los primeros datos reales después de aplicar la migración
de integridad, pero **la operación todavía no está lista para declarar margen FIFO**. Hay
dos bloqueadores de datos:

1. Asociar los productos vendidos de Epos con `productos_menu`, recetas y productos de
   inventario. Mientras los IDs no estén resueltos, las ventas deben quedar como excepción.
2. Definir y registrar el costo base de la apertura como lotes FIFO. El snapshot 52 tiene
   cantidades físicas y valor histórico, pero la producción no tiene lotes de apertura.
   Crear esos lotes es una escritura de producción y requiere respaldo y confirmación del
   criterio de costo; no se ejecutó automáticamente.

No se aplicaron migraciones ni se escribieron datos en producción durante esta auditoría.
La prueba real puede comenzar con captura de tickets y conciliación Epos, pero el consumo
FIFO sólo debe confirmarse después de resolver esos dos bloqueadores.
